name: Monthly CTL Report

on:
  schedule:
    - cron: '0 8 1 * *'
  workflow_dispatch:
    inputs:
      month:
        description: 'Month to report (YYYY-MM). Leave blank for previous month.'
        required: false
        default: ''

permissions:
  contents: write

jobs:
  build-report:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r orm-reports/wmchealth-ctl-reports/requirements.txt

      - name: Fetch Press Ganey data
        env:
          PG_APP_ID: ${{ secrets.PG_APP_ID }}
          PG_APP_SECRET: ${{ secrets.PG_APP_SECRET }}
          PG_NODE_ID: ${{ secrets.PG_NODE_ID }}
        run: |
          MONTH="${{ github.event.inputs.month }}"
          if [ -z "$MONTH" ]; then
            python orm-reports/wmchealth-ctl-reports/scripts/fetch_data.py \
              --output orm-reports/wmchealth-ctl-reports/scripts/data.json
          else
            python orm-reports/wmchealth-ctl-reports/scripts/fetch_data.py \
              --month "$MONTH" \
              --output orm-reports/wmchealth-ctl-reports/scripts/data.json
          fi

      - name: Build PowerPoint report
        run: |
          python orm-reports/wmchealth-ctl-reports/scripts/build_report.py \
            --data orm-reports/wmchealth-ctl-reports/scripts/data.json \
            --output orm-reports/wmchealth-ctl-reports/docs/reports/WMCHealth_CTL_report.pptx

      - name: Update dashboard
        run: |
          python orm-reports/wmchealth-ctl-reports/scripts/update_dashboard.py \
            --data orm-reports/wmchealth-ctl-reports/scripts/data.json

      - name: Commit and push report
        run: |
          git config --global user.name  "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add orm-reports/
          git diff --staged --quiet || git commit -m "📊 Auto-generate CTL report $(date +'%B %Y')"
          git push
